# Stage 5 evaluation — VERDICT: ACCEPT (2026-07-17, evaluator window)

Evaluated per the run-stage eval protocol. **Timing context:** the builder ran
2026-07-14 (15:35–15:38), BEFORE the E-NIF battery (07-17) and before several
evaluator-side data corrections (L21 dedup, manual-table dedup, corpus 220→232).
The stage is judged against its spec and its data state; post-hoc supersessions
are mapped as Stage-6 inputs, not failures.

## 1. Reproducibility — PASS (one post-hoc break, fixed, not the builder's fault)
- `stage5_law` re-run against the CURRENT corpus (232 rows): every frozen
  constant re-derives with **0.0% drift** — K-table, G_w(T1–T6), G_l(Inf T1–T10),
  G_l(Lan,T2)=0.654, factorization f={1, 6.83, 7.51}. The constants are robust
  to all corrections that landed after the build.
- `stage5_validate` re-runs clean: composition anchors 5/5 + naked pair, mop-up
  16/16, thresholds shown-not-fit, per-row sections consistent with the report.
- `stage5_composition` **crashed on re-run** (`ladder[1]` KeyError) — caused by
  the EVALUATOR's post-build dedup of the manual table (N=1/N=2 rows moved to
  JSON-backed). Fixed 2026-07-17 with a dated, data-sourcing-only patch (pull
  those rungs from their corpus JSON rows); outputs after the fix reproduce the
  report exactly (ratios 0.4231/0.6923/0.7308; mop-up 16/16 OK).
- Predictor suite: **105 passed, 8 skipped, 2 xfailed** (matches report).
- **Backtest gate G12: PASS** — no locked battle regressed (matches report).

## 2. Independent recomputation (own arithmetic) — PASS
- **G_l(Lancer,T2)**: both corrected T2-Lancer rows → 0.6542 / 0.6539 — two
  independent battles, different attacker panels, same value to 3 decimals ✓.
- **Ceil-exact FarSeer row** (T1MMvT6Inf): pred 5.469 → ceil 6 = observed 6 ✓.
- **Seam smoke**: 12.524·D·H/(A·L) = 91.76 → 92 vs observed 92/92 ✓.
- Cross-checks vs my own E-NIF work (computed independently, after the build):
  K(MM→Inf) builder 91.16 vs my R02 measurement 90.1 ✓; the builder's §5
  shield-gating hypothesis for the count thresholds is independently supported
  by the E-NIF S_gatot decay measurement (4.7→2.2→1.2).

## 3. Guardrails — PASS
- **api.py wiring is additive-only**: exactly two new lazily-imported entry
  points (`predict_deterministic_1v1/battle`); zero modified lines touch
  `predict()`; `server.py` behavior unchanged; G12 unchanged. Scope honored.
- **No fabrication**: every reported number regenerated by the three scripts.
- **No outcome leakage**: constants derive from measured tables; validation is
  stats-in. The `anchor` composition mode (uses measured t_solo) is clearly
  separated from `law` mode.
- **Failures reported as failures** — exemplary: the law-anchored solo-vs-duo
  miss (~20 vs 78) is kept visible and flagged proc-gated; non-Inf-dealer
  cross-regime band honestly stated at ±30%; factorized cells labelled
  ±10–15% directional; count thresholds excluded as out-of-scope rather than
  fitted. The builder also SELF-CORRECTED its own earlier wrong claim (the
  "FarSeer L/H missing" flag — withdrawn with the real cause, integer-turn
  quantization, 15/17 ceil-exact).
- **Genuine new science in-stage**: the integer-turn/ceil gate; the
  cadence-limited mop-up law `front + max(ceil(4j/3), latency)` (16/16);
  G_l(Lancer,T2)=0.654 double-measured; regime-independent K anchor
  (12.52/12.17/12.31 across three hero regimes).

## 4. Post-build supersessions (NOT failures — the Stage-6 work list)
1. **G_w is class-dependent** (E-NIF, 07-17): Stage 5 applies the Infantry G_w
   table to ALL dealer classes — the builder itself flagged this as a
   hypothesis on non-Inf dealers. Measured since: Lancer 1/1.09/1.63 (mild),
   Marksman ~flat (≤1.17 at T6). Stage 6 must key G_w by dealer class. (No
   in-sample damage: every validated non-Inf dealer row was T1.)
2. **Gatot-kit suppression is now measured** (S_gatot ≈ 4.7/2.2/1.2 at
   T1/T3/T6 dealer strength, saturating) — replaces the binary "proc-gated"
   flag with a quantifiable curve; also the likely mechanism behind §5's
   knife-edge thresholds, exactly as the builder hypothesized.
3. **Alliance buff** (+23pp A/D, +10pp L/H) discovered as a real panel
   variable — capture-protocol rule + a Stage-6 input check.
4. **L21 ruling**: the builder validated corrected-L21 as a distinct row
   (+26%); Martin subsequently ruled it a duplicate capture of L19 — now
   excluded at the ledger. The builder's registry entry for it is an orphan
   (matches zero rows) — harmless; tidy in Stage 6.
5. The builder's §7.5 "pre-existing flags stand" — all four have since been
   RESOLVED (T7 cross-validated; 2-named-MM genuine incl. new JSONs; L32/L69
   dissolved via the counter copy-paste fix; T6vT1=96 was a T4 relabel).

## Verdict
**ACCEPT.** The frozen law reproduces from the corpus with zero drift even
after a week of data corrections; validation is honest, quantified, and
bucketed by instrument; the seam wiring is additive and gated; failures and
hypotheses are labelled as such throughout. Stage 5 delivers exactly what its
spec asked: the per-unit law + composition algorithm frozen in code, validated,
and wired. The supersession map above is the opening scope of `STAGE6_SPEC.md`.
