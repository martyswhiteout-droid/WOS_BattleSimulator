# Python
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# OS / editor cruft
.DS_Store
Thumbs.db

# Local scratch
*.log

.vercel
.env*
!shell/.env.example

# Generated release bundles (rebuild via shell/promote.py) — duplicates the
# whole data tree inside an artifact snapshot; also trips Windows MAX_PATH.
shell/build/
