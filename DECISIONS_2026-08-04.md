# DECISIONS — overnight build 2026-08-04 (for Martin's morning review)

Every judgment call made under delegated authority tonight. Each is reversible; reversal cost noted. Nothing deployed anywhere; WOSTests.com untouched (still empty); prototype engine/UI untouched.

| # | Decision | Why | Alternatives (morning options) | Reversal cost |
|---|---|---|---|---|
| 1 | **Execute shell build without the Stage 3–5 red-team round** (your directive) | You ordered it; building mock-first means red-team can attack the BUILT shell before launch, which is cheaper to fix than a paper plan anyway | Run red-team NOW against the built shell (recommended before Phase 0 keys go in): I can produce the technical brief + red-team prompt on request | None — red-team later loses nothing tonight |
| 2 | **Shell lives at `E:\WOS\Battle Simulator\shell\`** (isolated subfolder) | Only two folders are mounted; WOSTests.com is forbidden (invariant 4). COMPASS invariant 5 ("no auth code in Battle Simulator") was written to protect the experimental loop — isolation preserves its intent: `shell/` never imports INTO `wos_sim/`, never edits `prototype/`, has its own deps/tests. COMPASS invariant 5 reworded accordingly | Separate repo/folder (needs you to mount it); keep invariant literal and park the code elsewhere | Move the folder; ~10 min |
| 3 | **Clerk hosted sign-in (Account Portal redirect), not embedded components** | Zero login UI to build/maintain; works with the vanilla-JS prototype unmodified; `__session` cookie verified server-side via JWKS | Embedded Clerk JS components (more control, more code); Supabase Auth (one vendor fewer); Auth0 (costlier) | Swap auth middleware; ~1 day |
| 4 | **Supabase = database ONLY; shell is the sole DB client (service-role, server-side)** | Browser never touches Supabase/DB → smaller attack surface, simpler mental model; RLS stays on as defense-in-depth | Client-side Supabase with RLS (more moving parts, no benefit here) | Low |
| 5 | **Stripe direct integration** (checkout + webhooks) | Full control of products/quotas; industry default | **Clerk Billing** (less code, Stripe under the hood); **Paddle** (merchant-of-record — handles global sales tax/GST for you; worth revisiting if tax filings become a burden) | Moderate; billing module is isolated |
| 6 | **Quota contradiction resolved to the LOWER pair: free 5 sims/day (0 OCR), paid 100 sims/day + 30 OCR/day** | COMPASS D2 recommendation: anti-extraction is the binding constraint; easier to loosen than tighten. PRODUCTION_CRITERIA D2 example numbers updated to match | Looser (20/200) for friendlier trials | Config row in `entitlements` table; minutes |
| 7 | **Paid tier priced US$7.99/mo (placeholder in configs/docs)** | Below one in-game pack; anchored to WoS player spend, not Western SaaS norms | $4.99 (volume play), $9.99 (+annual discount), or regional pricing later | Stripe product config; minutes |
| 8 | **OCR: Anthropic vision API primary, Gemini fallback; built mock-first tonight** | Best structured extraction with the v2 schema; never-fabricate rule enforced in prompt + validator | Gemini primary (cheaper); OpenAI | Swap client class; hours |
| 9 | **Asset pack v1 = code-drawn original SVG class/generation emblems** | Image-gen API unavailable tonight; SVG emblems are clean-room original, scale perfectly, tiny | Commission artist; AI-generate original (non-derivative) art later — pack is swappable by design | Drop-in replacement anytime |
| 10 | **COMPASS.md updated**: shell BUILD unblocked (your 08-04 directive recorded); LAUNCH still gated by QA PASS + your sign-off + D4 certify-narrow scoping | Docs must not contradict reality (that's how the stale traps happened) | — | — |
| 11 | **BRD.md supersession banner applied** (COMPASS D5 — you hadn't objected; it's the recommended, cheap, reversible option) | Stops agents building to the single-user CLI spec | Remove banner if you disagree | Delete 4 lines |
| 12 | **Century Games letter DRAFTED, NOT sent** (COMPASS D3 followed) | Ship-clean-first, decide after; sending is explicitly yours | Send it; or never | — |
| 13 | **Prototype read-only tonight** — no engine/UI/calibration changes; COMPASS D1 (non-wipe experiment campaign) and D4 (certify-narrow) remain YOUR open decisions | They're gameplay/strategy calls with real troop costs — not delegable | — | — |
| 14 | **Cloudflare free tier recommended in front of the VPS** (not active — needs your registration) | Only realistic DDoS answer on a single VPS | Skip it; accept the risk documented in plan §5 | — |

**What tonight explicitly did NOT do:** deploy anything · touch WOSTests.com · create any third-party account · modify `wos_sim/` or `prototype/` · send any external communication.
